# Pour (`pour`) — materials

This scene isn't on `main`: its code and its record (`docs/scenes/pour.md`) live in PR #122 (closed without merging). What it was built with is kept here so nothing is lost either way.

- `pour/pour-still/` — our renders of the scene and the scripts that measured and shot it. The pasted reference still it was built from stays out of this repo (it's in the local `tools/.cache/refs/pour-still/` and the private archive).
